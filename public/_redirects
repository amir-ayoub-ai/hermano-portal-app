# Stub temporário até a API subir no Coolify amanhã.
# Faz /api/* responder 404 imediatamente em vez de hang/timeout no DNS de
# api.hermanocorradi.com.br (que ainda não existe). AuthContext recebe
# ApiError(404), trata como erro genérico e cai gracefully no Login.
# (CF Pages só aceita 200/301/302/303/307/308/404/410/451 em _redirects.)
/api/*    /api-stub.json    404

# SPA fallback do React Router (mantido)
/*        /index.html       200
